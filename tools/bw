if ! _which bw; then
  pushd /tmp > /dev/null
  curl -sLo- 'https://bitwarden.com/download/?app=cli&platform=linux' > /tmp/bw.zip
  unzip -o /tmp/bw.zip
  chmod +x bw
  eval "$SUDO mv bw /usr/bin/bw"
  popd > /dev/null
fi